pluginManagement {
    repositories {
        mavenCentral()
        gradlePluginPortal()
        maven { url = uri('https://maven.neoforged.net/releases/') }
    }
}

rootProject.name = 'direct-thaumcraft-port'
